<?php
/**
 * @file
 * Code for the iasc_discussion feature.
 */

include_once 'iasc_discussion.features.inc';

/**
 * Implements hook_ctools_plugin_directory().
 */
function iasc_discussion_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'ctools' && $plugin_type == 'content_types') {
    return 'plugins/content_types';
  }
}

